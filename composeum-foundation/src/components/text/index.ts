import { ComposeumComponent } from "composeum-react"
import schema from "./text.schema"
import component from "./text"

export const Text: ComposeumComponent = {
  component: component,
  schema: schema,
}
