import { ComposeumComponent } from "composeum-react"
import schema from "./homepage.schema"
import component from "./homepage"

export const Image: ComposeumComponent = {
  component: component,
  schema: schema,
}
